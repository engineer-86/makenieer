version: '3.8'
services:
  makenieer-web:
    image: registry.gitlab.com/engineer86/makenieer:v0401b
    container_name: makenieer_web
    ports:
      - "80:8000"
    environment:
      - APP_ENV=production
